apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: basic-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: workep-anycast
spec:
  rules:
  - host: mixtapeapi.vobi.io
    http:
      paths:
      - backend:
          serviceName: mixtapeapi
          servicePort: 8001
